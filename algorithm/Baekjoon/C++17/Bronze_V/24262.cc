#include <iostream>
using namespace std;

int main() {
    cin.tie(NULL);
    ios_base::sync_with_stdio(false);

    int n;
    cin >> n;

    cout << 1 << '\n';  // floor func. -> return...
    cout << 0 << '\n';  // O(1)

    return 0;
}