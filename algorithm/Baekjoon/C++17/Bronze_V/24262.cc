#include <iostream>

using namespace std;

int main() {
    cin.tie(nullptr);
    ios_base::sync_with_stdio(false);

    cout << 1 << '\n';  // floor func. -> return...
    cout << 0 << '\n';  // O(1)

    return 0;
}